# Gryphon's `~/.conf` Directory

The `~/.conf` directory contains bash-based tooling and enviornment
configurations and utilities.

    ./install   # Installs everything
    ./uninstall # Uninstalls everything
